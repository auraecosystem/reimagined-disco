class QVector(list):

    def sum(self):
        return sum(self)

    def average(self):
        return sum(self) / len(self)

    def map(self, fn):
        return QVector(
            [fn(x) for x in self]
        )
